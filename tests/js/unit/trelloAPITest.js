// TEST STARTS HERE
const chai = require('chai');
const chaiHttp = require('chai-http');

const expect = chai.expect;
chai.use(chaiHttp);

describe('Trello API 사용이 가능하다', async () => {
  it('트렐로에 로그인하는 것이 가능하다', async (done) => {
    chai.request(url)
  })
  it('뭐가 또 가능하다. ', async(done) => {
    
  })

})
